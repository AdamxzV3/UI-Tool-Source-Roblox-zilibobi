return {
	-- scrolling smooth factor
	scrollingFactor = 0.3,

	-- button effects
	buttonDampingRatio = 0.5,
	buttonUndampedFrequency = 3,

	buttonPositionChange = 2,
	buttonSizeChange = 1,

	buttonEffectDelay = 0.1,

	-- lock state (variable)
	lock = false,
	lockWindow = "",

	-- accent (variable)
	accent = "red",
}
